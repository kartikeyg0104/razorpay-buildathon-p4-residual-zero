# eval-diff `artifacts/v1` → `artifacts/v1`

0 disposition change(s).

| credit_id | from | to |
|---|---|---|
| — | — | — |

Self-diff of the tagged Phase 1 A3 map (`248` credits, all `FLAGGED`). Filed so
the F54 rule has a first attachment. A later flags-on eval that moves any credit
must replace this file and link it from `docs/EVALUATION.md`.
