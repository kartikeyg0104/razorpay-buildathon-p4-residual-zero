# eval-diff `artifacts/v1` → `artifacts/dev`

0 disposition change(s).

| credit_id | from | to |
|---|---|---|
| — | — | — |

Flags-on Gate 2 A3 map versus the tagged Phase 1 baseline. F31 cap-refusal left
every credit `FLAGGED`; the autonomy threshold was not moved. Empty diff is the
measurement, not a missing file.
