# DATA.md

_Stub created at CP0. Filled at CP1 with the generator's design, the realised corpus counts, and
the "assumptions we are least confident about" section §8.1 requires._

The corpus is synthetic and this document is where that is documented rather than glossed. It must
carry, at CP1:

- the four generator stages and why inverting them destroys the answer key;
- the Phase 1 merchant profile's parameter values;
- realised counts: credits and ledger items per split;
- the distribution of `m`, the sub-rupee member count, which is the quantity the search tolerance
  is derived from;
- an explicit "assumptions we are least confident about" section.
