# LLM runtime. Credentials never live here; they come from the environment.
# Q2 answer C: eval / A3 stay stub. Optional Ask / AI finance controller:
# NVIDIA_API_KEY or AI_API_KEY in .env. AI_PROVIDER=stub forces fallback templates.
# NVIDIA NIM is the only supported live provider (Groq removed 2026-09-03).
# Tier 4 is unexercised; F6 reports the tier 1–3 mix.

model_id: stub
effort: none
prompt_version: 1
token_budget: 0
rapidfuzz_threshold: 85
top_two_margin: 5
shortlist_k: 5
cache_dir: data/cache/llm
