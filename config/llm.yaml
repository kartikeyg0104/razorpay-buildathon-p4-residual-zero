# LLM runtime. Credentials never live here; they come from the environment.
# Q2 answer C: no spend in Phase 1. The client is a stub. Dev runs --offline.
# Tier 4 is unexercised; F6 reports the tier 1–3 mix.

model_id: stub
effort: none
prompt_version: 1
token_budget: 0
rapidfuzz_threshold: 85
top_two_margin: 5
shortlist_k: 5
cache_dir: data/cache/llm
